{
    "ipv4addr": "{{ address }}",
    "mac": "{{ mac_addr }}",
    "name": "{{ host }}",
    "comment": "{{ comment }}",
    "options":
        [
            {
                "name": "dhcp-lease-time",
                "num": 51,
                "use_option": false,
                "value": "43200",
                "vendor_class": "DHCP"
            }
        ]
}
